// create a function that finds the numberof a word in an array

let numberOforange = 0

const theWordToFind = "orange"

const list1 = ["orange", "banana", "orange", "apple", "orange", "banana"]
const list2 = ["palm", "fdgg", "grg", "hrrhr", "hrhr", "baneggana"]

function findNumberOfWordInAList() { theWordToFind, list}){
    let numberOforange = 0;
}


    for (let i = 0; i < words.length; i++) {
        if (words[i] === "theWordToFind") {
            numberOforange++;
        }

    }
    let numberOforange = 0
}

findNumberOfWordInAList("orange", list1)
findNumberOfWordInAList("orange", list2)

findNumberOfWord()
findNumberOfWord()


console.log(numberOforange)